name: Q3

on:
  workflow_dispatch:
  push:
    branches: [ main ]

jobs:
  archive-java:
    runs-on: ubuntu-latest

    permissions:
      contents: write

    steps:
      - name: Récupérer code
        uses: actions/checkout@v4

      - name: Créer Q3_archive_java.txt avec nom et Q3.java
        run: |
          echo "Yachir Yanis" > Q3_archive_java.txt
          echo "Contenu de Q3.java :" >> Q3_archive_java.txt
          cat Q3.java >> Q3_archive_java.txt

      - name: Configurer Git
        run: |
          git config user.name "woldenps"
          git config user.email "wolden7620@gmail.com"

      - name: Commit et push fichier archivé
        run: |
          git add Q3_archive_java.txt
          git commit -m "Archive automatique de Q3.java"
          git push
